# MCP Tools

This directory is kept for the repository layout expected by operators.

Runtime tool implementation lives in `mem0_local_platform_mcp/` to avoid a
Python package-name collision with the upstream MCP SDK package named `mcp`.

